{
  "title": "StreamLoom Markets",
  "editable": true,
  "panels": [
    {
      "type": "graph",
      "title": "Ingested Trades",
      "targets": [
        {"expr": "rate(events_published_total[1m])"}
      ],
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0}
    },
    {
      "type": "graph",
      "title": "ClickHouse Inserts Latency",
      "targets": [
        {"expr": "histogram_quantile(0.95, sum(rate(insert_latency_ms_bucket[5m])) by (le))"}
      ],
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0}
    },
    {
      "type": "graph",
      "title": "API Request Rate",
      "targets": [
        {"expr": "rate(http_requests_total[1m])"}
      ],
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 8}
    }
  ],
  "schemaVersion": 38,
  "version": 1
}
